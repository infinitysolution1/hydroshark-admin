import React from "react";

export const metadata = {
  title: "Carts | Hydroshark",
  description: "India's First Carbonated Hydration Drink",
};

const Layout = ({ children }) => {
  return <main>{children}</main>;
};

export default Layout;
